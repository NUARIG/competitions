en:
  activerecord:
    attributes:
      criteria_reviews:
        base: ''
        score:
    errors:
      models:
        criteria_review:
          attributes:
            base:
              required_score: "'%{criterion}' must be scored."
              comment_not_shown: "Comments are not allowed on '%{criterion}'."
              criteria_not_from_grant: "'%{criterion} (ID: %{criterion_id}) is not a valid criterion for this grant."
  helpers:
    label:
      review:
        criteria_reviews:
          score: Score
          comments: Comments
