en:
  activerecord:
    attributes:
      grant_permission:
        user: User
    models:
      grant_permission:
        one: permission
        other: permissions
    errors:
      models:
        grant_permission:
          attributes:
            user:
              blank: must be selected
            role:
              blank: must be selected

