en:
  activerecord:
    attributes:
      grant:
        slug: Short name
    errors:
      messages:
        blank: is required.
      models:
        grant_permission:
          attributes:
            user:
              blank: must be selected.
            role:
              blank: must be selected.
