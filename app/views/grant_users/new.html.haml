- content_for(:page_title, 'New Grant User')

.grid-x
  .cell
    %h1 New grant user

.grid-x
  .cell
    = form_with model: [@grant, @grant_user], local: true do |f|
      = render 'new_form', f: f, grant: @grant, users: @users
