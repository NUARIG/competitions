- content_for(:page_title, 'Create a New Grant')
%h1 Create a New Grant

#remote_form_messages

=# form_with model: @grant, remote: true do |f|
= form_with model: @grant, local: true do |f|
  = render 'default_set_select', f: f
  = render 'form', f: f, grant: @grant

= link_to 'Back', grants_path
