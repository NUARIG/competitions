- content_for(:page_title, "#{@grant.name}")

%h1= "Editing #{@grant.name}"

= form_with model: @grant, local: true do |f|
  = render 'form', f: f, grant: @grant, questions: @questions

  = link_to 'Show', @grant, id: "grant-#{@grant.id}"
  |
  = link_to 'Back', grants_path
