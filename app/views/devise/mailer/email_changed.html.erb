<p>Hello <%= @email %>!</p>

<% if @resource.try(:unconfirmed_email?) %>
  <p><%= "We're contacting you to notify you that your account email in #{COMPETITIONS_CONFIG[:application_name]} is being changed to #{@resource.unconfirmed_email}" %>.</p>
<% else %>
  <p><%= "We're contacting you to notify you that your account email in #{COMPETITIONS_CONFIG[:application_name]} is being changed to #{@resource.email}" %>.</p>
<% end %>
