- content_for(:page_title, 'Create a New Banner')

%h1
  Create a New Banner

= form_with model: @banner, local: true, data: { turbo: false } do |f|
  = render 'form', f: f, banner: @banner

= link_to 'Back to All Banners', banners_path
